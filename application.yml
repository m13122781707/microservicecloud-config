spring:
  profiles:
    active: dev

---
spring:
  profiles: test
  application:
    name: microservicecloud-config-test

---
spring:
  profiles: dev
  application:
    name: microservicecloud-config-dev